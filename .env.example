# Default: frontend uses the API served from the same origin.
# Optional separate backend (set this in the shell environment before starting/building):
# VITE_REMOTE_BACKEND_URL=http://127.0.0.1:5050
