<script setup>import { computed } from 'vue'; import { mockAnalysis } from '../data/mockAnalysis'; const props = defineProps({ selectedText: String, result: Object }); const analysis = computed(() => props.result || mockAnalysis.word)</script><template><section class="analysis"><h3>释义</h3><label>原句</label><blockquote>{{ selectedText || '请先选中正文' }}</blockquote><p>{{ analysis.items?.[0]?.meaning || analysis.meaning }}</p><small>{{ analysis.items?.[0]?.note || analysis.note }}</small></section></template>
