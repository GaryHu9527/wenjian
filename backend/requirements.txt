Flask>=3.0,<4.0
Flask-Cors>=4.0,<5.0
python-dotenv>=1.0,<2.0
requests>=2.31,<3.0
gunicorn>=22.0,<24.0
# Optional classical-Chinese NLP adapter. Its KenLM runtime and four model files
# are installed separately; the application degrades gracefully when absent.
jiayan==0.0.21
